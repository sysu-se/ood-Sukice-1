# Homework 2 Evolution Report

## 1. 你如何实现提示功能？

提示功能其实就两部分：一是算出某个空格子还能填哪些数，二是找到那个只有唯一候选数的格子当作下一步。

我把 `getCandidates` 和 `getNextHint` 这两个方法直接做在 `Sudoku` 对象里了。它们就是根据行、列、宫格的约束条件，把候选数一个个排除掉。这个逻辑本来就属于数独规则的一部分，放在领域对象里比放在 UI 层合理。

## 2. 你认为提示功能更属于 Sudoku 还是 Game？为什么？

我觉得提示功能应该属于 `Sudoku`。

因为它只跟当前棋盘状态有关，纯粹是数学计算，不涉及任何游戏历史或用户操作记录。`Game` 的职责是管理会话、追踪操作历史、处理撤销重做这些。提示只是一个只读的"查询"动作，不改变任何状态，所以放在 `Sudoku` 更合适。

## 3. 你如何实现探索模式？

我用了一个"状态帧栈"的思路。

当用户点击探索时，我把当前局面克隆一份，然后把这次探索的所有操作（guess、undo、redo）都限制在这个克隆的副本里，不会影响到主局面。探索栈里可以嵌套多层，这样支持树状的探索分支。

具体来说，`startExplore` 会把当前状态压入栈，`commitExplore` 把探索结果合并回主历史，`cancelExplore` 直接丢弃探索状态。`failedPaths` 会记录死胡同的棋盘状态，避免用户重复踩坑。

## 4. 主局面与探索局面的关系是什么？

探索局面是主局面的一份深拷贝。

两个局面完全隔离，探索期间的操作不会弄脏主局面的数据。提交的时候，我把探索的最终状态合并到主历史里作为一次原子操作；取消的时候就直接丢弃探索栈，所有改动都没了。

## 5. 你的 history 结构在本次作业中是否发生了变化？

变了。原来是线性的 undo/redo 栈，现在变成了栈套栈的结构。

主线上有一套 history，每个探索分支也有自己独立的 history。这样探索分支里的撤销不会影响到主线的进度，分支之间也互不干扰。

## 6. Homework 1 中的哪些设计，在 Homework 2 中暴露出了局限？

HW1 的时候 `Game` 的 `guess` 直接操作 `current` 和 `undoStack`，这种紧耦合的设计在需要"沙盒探索"的时候就显得不够灵活了。

HW1 的结构不支持把一段时间内的多次操作打包成一次原子动作，也不支持在同一局面下开多个并行的探索分支。

## 7. 如果重做一次 Homework 1，你会如何修改原设计？

我会考虑把历史管理这部分从 `Game` 里单独抽出来，做成一个 `HistoryManager` 或者备忘录模式的实现。

这样 `Game` 只需要负责协调，不需要自己维护一套 undo/redo 的细节逻辑。后面如果要加分支探索、多层嵌套这些功能，只需要扩展 HistoryManager 就好了，Game 本身的代码不用大改。
